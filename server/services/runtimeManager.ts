import { execFile, spawn, type ChildProcessWithoutNullStreams } from 'node:child_process';
import fs from 'node:fs';
import http from 'node:http';
import net from 'node:net';
import os from 'node:os';
import path from 'node:path';
import { WorkspaceManager } from './workspaceManager.js';
import { ExecutionWorker } from './executionWorker.js';
import { StorageGuard } from './storageGuard.js';

export type RuntimeStatus = 'static' | 'starting' | 'installing' | 'running' | 'error' | 'stopping' | 'stopped';
export interface RuntimeInfo { status:RuntimeStatus; framework?:string; packageManager?:string; command?:string; port?:number; url?:string; startedAt?:string; updatedAt:string; lastError?:string; errorCode?:string; output?:string; }
interface RuntimeRecord extends RuntimeInfo { child?:ChildProcessWithoutNullStreams; operation?:Promise<RuntimeInfo>; runtimeDir?:string; }
const records=new Map<string,RuntimeRecord>();
const MAX_OUTPUT=12000, START_TIMEOUT_MS=60000, INSTALL_TIMEOUT_MS=180000, PORT_ATTEMPTS=5;
const now=()=>new Date().toISOString();
const clip=(current:string|undefined,chunk:Buffer|string)=>`${current||''}${chunk.toString()}`.slice(-MAX_OUTPUT);
const hasFile=(cwd:string,file:string)=>fs.existsSync(path.join(cwd,file));
const readPackage=(cwd:string):any|null=>{try{return JSON.parse(fs.readFileSync(path.join(cwd,'package.json'),'utf8'));}catch{return null;}};
async function findPort(){return await new Promise<number>((resolve,reject)=>{const server=net.createServer();server.listen(0,'127.0.0.1',()=>{const address=server.address();const port=typeof address==='object'&&address?address.port:0;server.close(()=>resolve(port));});server.on('error',reject);});}
function detectPackageManager(cwd:string){if(hasFile(cwd,'pnpm-lock.yaml'))return'pnpm';if(hasFile(cwd,'yarn.lock'))return'yarn';if(hasFile(cwd,'bun.lockb')||hasFile(cwd,'bun.lock'))return'bun';return'npm';}
export function detectFramework(pkg:any){const deps={...(pkg?.dependencies||{}),...(pkg?.devDependencies||{})};if(deps.vite||String(pkg?.scripts?.dev||'').includes('vite'))return'vite';if(deps.next||String(pkg?.scripts?.dev||'').includes('next'))return'next';if(deps['@sveltejs/kit'])return'sveltekit';if(deps.astro)return'astro';if(deps['react-scripts'])return'cra';return'node';}
function toolCommand(pm:string){if(pm==='npm'){const npmCli=path.join(path.dirname(process.execPath),'node_modules','npm','bin','npm-cli.js');if(fs.existsSync(npmCli))return{command:process.execPath,prefix:[npmCli]};return{command:process.platform==='win32'?'npm.cmd':'npm',prefix:[]};}if(pm==='pnpm')return{command:process.platform==='win32'?'pnpm.cmd':'pnpm',prefix:[]};if(pm==='yarn')return{command:process.platform==='win32'?'yarn.cmd':'yarn',prefix:[]};return{command:process.platform==='win32'?'bun.exe':'bun',prefix:[]};}
function installArgs(pm:string,cwd:string){if(pm==='npm')return[...(hasFile(cwd,'package-lock.json')||hasFile(cwd,'npm-shrinkwrap.json')?['ci']:['install']),'--ignore-scripts','--no-audit','--no-fund'];if(pm==='pnpm')return['install',hasFile(cwd,'pnpm-lock.yaml')?'--frozen-lockfile':'--no-frozen-lockfile','--ignore-scripts'];if(pm==='yarn')return[...(hasFile(cwd,'yarn.lock')?['install','--frozen-lockfile']:['install']),'--ignore-scripts'];return['install','--ignore-scripts'];}
function startArgs(pm:string,pkg:any,framework:string,port:number){const script=pkg?.scripts?.dev?'dev':pkg?.scripts?.start?'start':'';if(!script)throw Object.assign(new Error('Nenhum script dev/start foi encontrado no package.json.'),{code:'PREVIEW_SCRIPT_MISSING'});const base=['run',script];if(framework==='vite')return[...base,'--','--host','127.0.0.1','--port',String(port),'--strictPort'];if(framework==='next')return[...base,'--','-H','127.0.0.1','-p',String(port)];if(framework==='astro')return[...base,'--','--host','127.0.0.1','--port',String(port)];return base;}
export function expectedBuildOutput(framework:string){if(framework==='vite'||framework==='astro')return'dist';if(framework==='cra')return'build';if(framework==='next')return'out';return'dist';}
function classifyRuntimeError(error:any,output=''){const text=`${String(error?.message||error||'')}\n${output}`;if(/ENOSPC|no space left|disk is full|database or disk is full/i.test(text))return{code:'PREVIEW_STORAGE_FULL',message:'Armazenamento insuficiente para preparar o preview.'};if(/ENOENT|not found|is not recognized|command not found/i.test(text))return{code:'PREVIEW_TOOL_MISSING',message:'O gerenciador de pacotes necessário para este projeto não está disponível no runtime.'};if(/EADDRINUSE|address already in use/i.test(text))return{code:'PREVIEW_PORT_BUSY',message:'A porta reservada para o preview ficou indisponível.'};if(/timed out|limite de tempo|não respondeu/i.test(text))return{code:'PREVIEW_TIMEOUT',message:'O servidor do projeto não respondeu dentro do limite de inicialização.'};if(/PREVIEW_SCRIPT_MISSING|Nenhum script dev\/start/i.test(text))return{code:'PREVIEW_SCRIPT_MISSING',message:'O package.json não possui um script dev ou start utilizável para preview.'};if(/npm ERR!|ERR_PNPM|yarn error|bun install/i.test(text))return{code:'PREVIEW_INSTALL_FAILED',message:'Falha ao instalar as dependências necessárias para o preview.'};return{code:String(error?.code||'PREVIEW_RUNTIME_FAILED'),message:String(error?.message||error||'Falha desconhecida no runtime do preview.')};}
export async function killProcessTree(child:ChildProcessWithoutNullStreams){if(child.killed||!child.pid)return;if(process.platform==='win32'){await new Promise<void>(resolve=>execFile('taskkill',['/pid',String(child.pid),'/t','/f'],{windowsHide:true},()=>resolve()));return;}try{process.kill(-child.pid,'SIGTERM');}catch{try{child.kill('SIGTERM');}catch{}}await new Promise(r=>setTimeout(r,400));try{process.kill(-child.pid,'SIGKILL');}catch{try{child.kill('SIGKILL');}catch{}}}
async function runTool(cwd:string,pm:string,args:string[],timeoutMs:number,signal?:AbortSignal,envOverride?:NodeJS.ProcessEnv){const{command,prefix}=toolCommand(pm);const started=Date.now();return await new Promise<{ok:boolean;output:string;durationMs:number;timedOut:boolean}>(resolve=>{const child=spawn(command,[...prefix,...args],{cwd,shell:false,windowsHide:true,detached:process.platform!=='win32',env:envOverride||ExecutionWorker.safeEnvironment()});let output='',settled=false,timedOut=false;const finish=(v:any)=>{if(settled)return;settled=true;clearTimeout(timer);signal?.removeEventListener('abort',abort);resolve(v);};child.stdout.on('data',b=>output=clip(output,b));child.stderr.on('data',b=>output=clip(output,b));const timer=setTimeout(()=>{timedOut=true;void killProcessTree(child);},timeoutMs);const abort=()=>void killProcessTree(child);signal?.addEventListener('abort',abort,{once:true});child.on('error',e=>finish({ok:false,output:clip(output,e.message),durationMs:Date.now()-started,timedOut:false}));child.on('close',code=>finish({ok:code===0,output,durationMs:Date.now()-started,timedOut:timedOut||Boolean(signal?.aborted)}));});}
export async function ensureDependenciesAt(cwd:string,signal?:AbortSignal){const pkg=readPackage(cwd);if(!pkg)return{ok:true,skipped:true,output:'Projeto sem package.json.',durationMs:0,packageManager:'none'};StorageGuard.assertCapacity('temporary','instalar dependências do preview');const pm=detectPackageManager(cwd);if(fs.existsSync(path.join(cwd,'node_modules')))return{ok:true,skipped:true,output:'Dependências já presentes.',durationMs:0,packageManager:pm};const result=await runTool(cwd,pm,installArgs(pm,cwd),INSTALL_TIMEOUT_MS,signal,ExecutionWorker.sandboxEnvironment(cwd));return{...result,skipped:false,packageManager:pm};}
async function waitForHttp(port:number,signal?:AbortSignal){const deadline=Date.now()+START_TIMEOUT_MS;while(Date.now()<deadline){if(signal?.aborted)throw new Error('Inicialização cancelada.');const ok=await new Promise<boolean>(resolve=>{const req=http.request({hostname:'127.0.0.1',port,path:'/',method:'GET',timeout:1500},res=>{res.resume();resolve(true);});req.on('error',()=>resolve(false));req.on('timeout',()=>{req.destroy();resolve(false);});req.end();});if(ok)return;await new Promise(r=>setTimeout(r,500));}throw new Error('O dev server não respondeu dentro do limite de tempo.');}
function safePreviewHeaders(headers:http.IncomingHttpHeaders){const blocked=new Set(['host','cookie','authorization','proxy-authorization','x-forwarded-for','x-forwarded-host','x-forwarded-proto','content-length']);const next:Record<string,string|string[]>={};for(const[key,value]of Object.entries(headers)){if(value===undefined||blocked.has(key.toLowerCase())||key.toLowerCase().startsWith('x-forge'))continue;next[key]=Array.isArray(value)?value:String(value);}next.host='127.0.0.1';return next;}
function writePreviewHeaders(upstream:http.IncomingMessage,res:http.ServerResponse){for(const[key,value]of Object.entries(upstream.headers)){if(value!==undefined&&!['content-security-policy','x-frame-options','set-cookie'].includes(key.toLowerCase()))res.setHeader(key,value as any);}res.setHeader('X-Frame-Options','SAMEORIGIN');res.setHeader('Content-Security-Policy',"sandbox allow-scripts; default-src 'self' https: data: blob:; script-src 'unsafe-inline' 'unsafe-eval' https:; style-src 'unsafe-inline' https:; connect-src 'self' https: wss: ws:; img-src 'self' https: data: blob:; font-src 'self' https: data:; form-action 'none'");}
function ephemeralRuntimeDir(projectId:string){return path.join(StorageGuard.previewTempRoot(),projectId);}
function prepareEphemeralRuntime(projectId:string){StorageGuard.assertCapacity('temporary','copiar o projeto para o preview');const source=WorkspaceManager.getProjectDir(projectId),target=ephemeralRuntimeDir(projectId);fs.rmSync(target,{recursive:true,force:true});fs.mkdirSync(target,{recursive:true});fs.cpSync(source,target,{recursive:true,filter:(src)=>{const rel=path.relative(source,src).replace(/\\/g,'/');return rel!=='node_modules'&&!rel.startsWith('node_modules/')&&!rel.startsWith('.git/');}});return target;}

export class RuntimeManager{
 static get(projectId:string):RuntimeInfo|null{const r=records.get(projectId);if(!r)return null;const{child:_c,operation:_o,runtimeDir:_d,...info}=r;return info;}
 static async build(projectId:string,signal?:AbortSignal){const cwd=WorkspaceManager.getProjectDir(projectId),pkg=readPackage(cwd);if(!pkg)throw new Error('Projeto sem package.json não possui build de framework.');const pm=detectPackageManager(cwd),framework=detectFramework(pkg);if(!pkg?.scripts?.build)throw new Error('Script build não declarado no package.json.');const result=await runTool(cwd,pm,['run','build'],INSTALL_TIMEOUT_MS,signal);const artifact=path.join(cwd,expectedBuildOutput(framework));return{ok:result.ok,output:result.output,framework,packageManager:pm,artifactDir:result.ok&&fs.existsSync(path.join(artifact,'index.html'))?artifact:undefined,durationMs:result.durationMs};}
 static async ensure(projectId:string,signal?:AbortSignal):Promise<RuntimeInfo>{const source=WorkspaceManager.getProjectDir(projectId);if(!readPackage(source))return{status:'static',updatedAt:now(),lastError:'Projeto sem package.json usa preview estático.'};const existing=records.get(projectId);if(existing?.status==='running'&&existing.child&&!existing.child.killed&&existing.port)return this.get(projectId)!;if(existing?.operation)return existing.operation;let cwd:string;try{cwd=prepareEphemeralRuntime(projectId);}catch(error:any){const classified=classifyRuntimeError(error);const info={status:'error' as const,updatedAt:now(),lastError:classified.message,errorCode:classified.code,output:String(error?.message||error).slice(-MAX_OUTPUT)};records.set(projectId,info);return info;}return this.ensureAt(projectId,cwd,signal,false);}
 static async ensureAt(key:string,cwd:string,signal?:AbortSignal,sandboxed=true):Promise<RuntimeInfo>{const pkg=readPackage(cwd);if(!pkg)return{status:'static',updatedAt:now(),lastError:'Projeto sem package.json usa preview estático.'};const existing=records.get(key);if(existing?.status==='running'&&existing.child&&!existing.child.killed&&existing.port)return this.get(key)!;if(existing?.operation)return existing.operation;const pm=detectPackageManager(cwd),framework=detectFramework(pkg);const operation=this.start(key,cwd,pkg,pm,framework,signal,sandboxed);records.set(key,{status:'starting',packageManager:pm,framework,updatedAt:now(),operation,runtimeDir:cwd});return operation;}
 private static async start(key:string,cwd:string,pkg:any,pm:string,framework:string,signal?:AbortSignal,sandboxed=false):Promise<RuntimeInfo>{let diagnosticOutput='';try{StorageGuard.assertCapacity('temporary','iniciar o runtime do preview');if(!fs.existsSync(path.join(cwd,'node_modules'))){const args=installArgs(pm,cwd);const current=records.get(key);records.set(key,{...current,status:'installing',packageManager:pm,framework,updatedAt:now(),command:`${pm} ${args.join(' ')}`,runtimeDir:cwd});const installed=await runTool(cwd,pm,args,INSTALL_TIMEOUT_MS,signal,sandboxed?ExecutionWorker.sandboxEnvironment(cwd):ExecutionWorker.safeEnvironment());diagnosticOutput=installed.output;if(!installed.ok){const timeoutMessage=installed.timedOut?'A instalação de dependências excedeu o limite de tempo. ':'';throw new Error(`${timeoutMessage}Falha ao instalar dependências: ${installed.output||'sem saída'}`);}}let lastError:unknown;for(let attempt=0;attempt<PORT_ATTEMPTS;attempt++){const port=await findPort(),args=startArgs(pm,pkg,framework,port),{command,prefix}=toolCommand(pm),env=sandboxed?ExecutionWorker.sandboxEnvironment(cwd,{PORT:String(port),HOST:'127.0.0.1',FORGE_PROJECT_RUNTIME:'1',FORGE_BROWSER_RUNTIME:'1'}):{...ExecutionWorker.safeEnvironment(),PORT:String(port),HOST:'127.0.0.1',FORGE_PROJECT_RUNTIME:'1'};const child=spawn(command,[...prefix,...args],{cwd,shell:false,windowsHide:true,detached:process.platform!=='win32',env});const record:RuntimeRecord={status:'starting',packageManager:pm,framework,command:`${pm} ${args.join(' ')}`,port,url:`http://127.0.0.1:${port}`,startedAt:now(),updatedAt:now(),child,output:'',runtimeDir:cwd};records.set(key,record);child.stdout.on('data',b=>{record.output=clip(record.output,b);record.updatedAt=now();});child.stderr.on('data',b=>{record.output=clip(record.output,b);record.updatedAt=now();});child.on('exit',code=>{if(!['stopping','stopped'].includes(record.status)){record.status=code===0?'stopped':'error';record.lastError=code===0?undefined:`Processo encerrado com código ${code}.`;record.errorCode=code===0?undefined:'PREVIEW_PROCESS_EXIT';record.updatedAt=now();}});const abort=()=>void this.stop(key);signal?.addEventListener('abort',abort,{once:true});try{await waitForHttp(port,signal);signal?.removeEventListener('abort',abort);record.status='running';record.updatedAt=now();return this.get(key)!;}catch(error:any){signal?.removeEventListener('abort',abort);lastError=error;diagnosticOutput=record.output||diagnosticOutput;const text=`${record.output||''}\n${String(error?.message||error)}`;await this.stop(key);if(!/EADDRINUSE|address already in use/i.test(text)||attempt===PORT_ATTEMPTS-1)throw error;}}throw lastError||new Error('Não foi possível reservar porta para o runtime.');}catch(error:any){await this.stop(key);const classified=classifyRuntimeError(error,diagnosticOutput);const info:RuntimeRecord={status:'error',packageManager:pm,framework,updatedAt:now(),lastError:classified.message,errorCode:classified.code,output:clip(diagnosticOutput,String(error?.message||error)),runtimeDir:cwd};records.set(key,info);console.error('FORGE_PREVIEW_RUNTIME_ERROR',JSON.stringify({projectId:key,framework,packageManager:pm,errorCode:classified.code,message:classified.message,detail:String(error?.message||error).slice(0,500)}));return this.get(key)!;}}
 static async stop(key:string):Promise<RuntimeInfo>{const record=records.get(key);if(!record)return{status:'stopped',updatedAt:now()};record.status='stopping';record.updatedAt=now();const child=record.child;if(child&&!child.killed)await new Promise<void>(resolve=>{const timer=setTimeout(()=>void killProcessTree(child).finally(resolve),5000);child.once('exit',()=>{clearTimeout(timer);resolve();});void killProcessTree(child).catch(()=>undefined);});const runtimeDir=record.runtimeDir;records.set(key,{...record,child:undefined,operation:undefined,status:'stopped',updatedAt:now()});if(runtimeDir&&runtimeDir.startsWith(StorageGuard.previewTempRoot()))try{fs.rmSync(runtimeDir,{recursive:true,force:true});}catch{}return this.get(key)!;}
 static async restart(projectId:string,signal?:AbortSignal){await this.stop(projectId);return this.ensure(projectId,signal);}
 static proxy(projectId:string,req:http.IncomingMessage,reqPath:string,res:http.ServerResponse){const record=records.get(projectId);if(record?.status!=='running'||!record.port)return false;const safePath=reqPath.startsWith('/')?reqPath:`/${reqPath}`;const request=http.request({hostname:'127.0.0.1',port:record.port,path:safePath,method:req.method,headers:safePreviewHeaders(req.headers)},upstream=>{res.statusCode=upstream.statusCode||200;writePreviewHeaders(upstream,res);upstream.pipe(res);});request.on('error',error=>{if(res.headersSent)return res.destroy(error as Error);res.statusCode=502;res.end(`Preview runtime indisponível: ${String((error as Error).message)}`);});if(!['GET','HEAD'].includes(String(req.method||'GET').toUpperCase()))req.pipe(request);else request.end();return true;}
 static async stopAll(){await Promise.all([...records.keys()].map(id=>this.stop(id)));}
}
